#!/bin/bash
echo "=== House of Tarab Backend — Deployment Guide ==="
echo ""
echo "The backend has been built and tested locally."
echo "Deploy to Render with these steps:"
echo ""
echo "Step 1: Push to GitHub"
echo "  cd ~/house-of-tarab-backend"
echo "  git add -A && git commit -m 'Initial backend with waitlist + Stripe checkout'"
echo "  gh repo create house-of-tarab-backend --public --push"
echo ""
echo "Step 2: Deploy on Render"
echo "  - Log in to https://dashboard.render.com"
echo "  - Click 'New +' > 'Web Service'"
echo "  - Connect your GitHub repo"
echo "  - Name: house-of-tarab-tickets"
echo "  - Environment: Node"
echo "  - Build Command: npm install"
echo "  - Start Command: npm start"
echo "  - Select Free plan"
echo ""
echo "Step 3: Add Environment Variables"
echo "  In Render dashboard:"
echo "  - NODE_ENV=production"
echo "  - BASE_URL=https://house-of-tarab-tickets-1.onrender.com (or your URL)"
echo "  - STRIPE_SECRET_KEY=sk_test_..."
echo "  - STRIPE_PUBLISHABLE_KEY=pk_test_..."
echo "  - STRIPE_WEBHOOK_SECRET=whsec_..."
echo ""
echo "Step 4: Update frontend API URLs"
echo "  The index.html and tickets.html fetch from /api/ endpoints."
echo "  Since they're served by the same Express backend, no changes needed."
echo "  Just make sure they're served from the same domain."
echo ""
echo "=== Backend API Endpoints ==="
echo "  POST /api/waitlist          — Add email to waitlist"
echo "  GET  /api/waitlist/count    — Get waitlist count"
echo "  POST /api/create-checkout-session — Create Stripe checkout"
echo "  POST /api/checkout          — Simple checkout (fallback)"
echo "  POST /api/stripe-webhook    — Stripe webhook handler"
echo "  GET  /api/health            — Health check"
echo ""
echo "=== Files ==="
echo "  server.js          — Main Express app"
echo "  db.js              — SQLite database (better-sqlite3)"
echo "  routes/waitlist.js — Waitlist API"
echo "  routes/checkout.js — Stripe checkout API"
echo "  public/index.html  — Landing page"
echo "  public/tickets.html — Ticket sales page"
echo "  render.yaml        — Render blueprint (optional)"
echo "  .env.example       — Environment variables template"